/**
 * Form Components Index
 *
 * Shared components for multi-step forms (GRN, Dispatch, Invoice).
 * Provides consistent form UI patterns across the app.
 */

export {
  FormStepWrapper,
  type FormStepWrapperProps,
  type StepConfig,
} from './FormStepWrapper';
